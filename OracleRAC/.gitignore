.git/
.vagrant/
.DS_Store
CVS
.vagrant
.zip
*.swp

# Per-project vagrant config may contain credentials — commit .example only.
*/config/vagrant.yml


# Installer-checksum verification stamps written by the Vagrantfile cache
*.verified
